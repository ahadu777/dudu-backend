name: Railway PR Preview

on:
  pull_request:
    types: [opened, synchronize, reopened]

permissions:
  contents: read
  pull-requests: write

env:
  RAILWAY_TOKEN: ${{ secrets.RAILWAY_TOKEN }}
  RAILWAY_PROJECT_ID: ${{ secrets.RAILWAY_PROJECT_ID }}

jobs:
  preview:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - name: Install Railway CLI and jq
        run: |
          curl -fsSL https://railway.app/install.sh | sh
          echo "$HOME/.railway/bin" >> $GITHUB_PATH
          sudo apt-get update && sudo apt-get install -y jq

      - name: Deploy PR Preview
        id: deploy
        env:
          PR_NUMBER: ${{ github.event.pull_request.number }}
          RAILWAY_TOKEN: ${{ secrets.RAILWAY_TOKEN }}
          RAILWAY_PROJECT_ID: ${{ secrets.RAILWAY_PROJECT_ID }}
        run: |
          SERVICE_NAME="api-pr-${PR_NUMBER}"
          
          echo "🚀 Deploying to service: ${SERVICE_NAME}"
          
          # Verify Railway token is set
          if [ -z "$RAILWAY_TOKEN" ]; then
            echo "❌ RAILWAY_TOKEN is not set"
            exit 1
          fi
          
          echo "✅ RAILWAY_TOKEN is set (length: ${#RAILWAY_TOKEN})"
          echo "✅ RAILWAY_PROJECT_ID: ${RAILWAY_PROJECT_ID}"
          
          # Create railway.toml with project ID for Railway CLI context
          cat > railway.toml << EOF
          project = "${RAILWAY_PROJECT_ID}"
          
          [build]
          builder = "DOCKERFILE"
          
          [deploy]
          startCommand = "npm start"
          healthcheckPath = "/healthz"
          EOF
          
          echo "✅ Created railway.toml with project context"
          
          # Check if service exists, create via Railway GraphQL API if it doesn't
          echo "🔍 Checking if service exists..."
          RAILWAY_GRAPHQL="https://backboard.railway.app/graphql/v2"
          
          # Query to list services
          LIST_QUERY='{"query":"query { project(id: \"'${RAILWAY_PROJECT_ID}'\") { services { id name } } }"}'
          SERVICES_RESPONSE=$(curl -s -X POST \
            -H "Authorization: Bearer ${RAILWAY_TOKEN}" \
            -H "Content-Type: application/json" \
            -d "$LIST_QUERY" \
            "$RAILWAY_GRAPHQL" 2>&1 || echo '{"data":{"project":{"services":[]}}}')
          
          SERVICE_EXISTS=$(echo "$SERVICES_RESPONSE" | jq -r ".data.project.services[] | select(.name == \"${SERVICE_NAME}\") | .id" 2>/dev/null || echo "")
          
          if [ -z "$SERVICE_EXISTS" ] || [ "$SERVICE_EXISTS" == "null" ]; then
            echo "📦 Service doesn't exist, creating via Railway API..."
            
            # Create service via GraphQL mutation
            CREATE_MUTATION='{"query":"mutation { serviceCreate(input: {name: \"'${SERVICE_NAME}'\", projectId: \"'${RAILWAY_PROJECT_ID}'\"}) { id name } }"}'
            CREATE_RESPONSE=$(curl -s -X POST \
              -H "Authorization: Bearer ${RAILWAY_TOKEN}" \
              -H "Content-Type: application/json" \
              -d "$CREATE_MUTATION" \
              "$RAILWAY_GRAPHQL" 2>&1 || echo "")
            
            SERVICE_ID=$(echo "$CREATE_RESPONSE" | jq -r '.data.serviceCreate.id // empty' 2>/dev/null || echo "")
            
            if [ -n "$SERVICE_ID" ] && [ "$SERVICE_ID" != "null" ]; then
              echo "✅ Service created successfully (ID: ${SERVICE_ID})"
              sleep 3
            else
              echo "⚠️ Service creation may have failed, but continuing..."
              echo "Response: ${CREATE_RESPONSE}"
            fi
          else
            echo "✅ Service already exists (ID: ${SERVICE_EXISTS})"
          fi
          
          # Set environment variables using Railway CLI
          echo "📝 Setting environment variables..."
          railway variables \
            --set "NODE_ENV=preview" \
            --set "PORT=3000" \
            --set "PR_NUMBER=${PR_NUMBER}" \
            --set "DB_NAME=myapp_pr_${PR_NUMBER}" \
            --set "JWT_SECRET=preview-secret-${PR_NUMBER}" \
            --service "$SERVICE_NAME" \
            --skip-deploys 2>&1 || echo "⚠️ Variables set attempted (may need service to exist first)"
          
          # Deploy with Railway CLI
          echo "🚀 Deploying service..."
          railway up --service "$SERVICE_NAME" --detach || {
            echo "❌ Deployment failed"
            exit 1
          }
          
          # Wait for deployment
          sleep 15
          
          # Railway auto-assigns domain
          PREVIEW_URL="https://${SERVICE_NAME}.up.railway.app"
          echo "url=${PREVIEW_URL}" >> $GITHUB_OUTPUT
          echo "✅ Preview URL: ${PREVIEW_URL}"

      - name: Comment PR with preview URL
        uses: actions/github-script@v7
        with:
          script: |
            const url = '${{ steps.deploy.outputs.url }}';
            const pr = context.issue.number;
            await github.rest.issues.createComment({
              ...context.repo,
              issue_number: pr,
              body: `## 🚀 Preview Environment Ready\n\n**API URL:** ${url}\n\n⚠️ Ephemeral environment, deleted on PR close.`
            });
