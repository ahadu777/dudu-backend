name: Railway PR Preview

on:
  pull_request:
    types: [opened, synchronize, reopened]

permissions:
  contents: read
  pull-requests: write

env:
  RAILWAY_TOKEN: ${{ secrets.RAILWAY_TOKEN }}
  RAILWAY_PROJECT_ID: ${{ secrets.RAILWAY_PROJECT_ID }}

jobs:
  preview:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - name: Install Railway CLI and jq
        run: |
          curl -fsSL https://railway.app/install.sh | sh
          echo "$HOME/.railway/bin" >> $GITHUB_PATH
          sudo apt-get update && sudo apt-get install -y jq

      - name: Deploy PR Preview
        id: deploy
        env:
          PR_NUMBER: ${{ github.event.pull_request.number }}
          RAILWAY_TOKEN: ${{ secrets.RAILWAY_TOKEN }}
          RAILWAY_PROJECT_ID: ${{ secrets.RAILWAY_PROJECT_ID }}
        run: |
          SERVICE_NAME="api-pr-${PR_NUMBER}"
          
          echo "🚀 Deploying to service: ${SERVICE_NAME}"
          
          # Verify Railway token is set
          if [ -z "$RAILWAY_TOKEN" ]; then
            echo "❌ RAILWAY_TOKEN is not set"
            exit 1
          fi
          
          echo "✅ RAILWAY_TOKEN is set (length: ${#RAILWAY_TOKEN})"
          echo "✅ RAILWAY_PROJECT_ID: ${RAILWAY_PROJECT_ID}"
          
          # Create railway.toml with project ID for Railway CLI context
          cat > railway.toml << EOF
          project = "${RAILWAY_PROJECT_ID}"
          
          [build]
          builder = "DOCKERFILE"
          
          [deploy]
          startCommand = "npm start"
          healthcheckPath = "/healthz"
          EOF
          
          echo "✅ Created railway.toml with project context"
          
          # Check if service exists, create via Railway GraphQL API if it doesn't
          # NOTE: This requires an Account Token (not Project Token)
          # Project Tokens cannot create services via API
          echo "🔍 Checking if service exists..."
          RAILWAY_GRAPHQL="https://backboard.railway.app/graphql/v2"
          
          # Query to list services (using correct GraphQL connection syntax)
          LIST_QUERY='{"query":"query { project(id: \"'${RAILWAY_PROJECT_ID}'\") { services { edges { node { id name } } } } }"}'
          SERVICES_RESPONSE=$(curl -s -X POST \
            -H "Authorization: Bearer ${RAILWAY_TOKEN}" \
            -H "Content-Type: application/json" \
            -d "$LIST_QUERY" \
            "$RAILWAY_GRAPHQL" 2>&1 || echo '{"data":{"project":{"services":{"edges":[]}}}}')
          
          # Check for errors
          if echo "$SERVICES_RESPONSE" | jq -e '.errors' > /dev/null 2>&1; then
            echo "⚠️ GraphQL query failed (may need Account Token):"
            echo "$SERVICES_RESPONSE" | jq '.errors' 2>/dev/null || echo "$SERVICES_RESPONSE"
            echo ""
            echo "💡 If using Project Token, services must be created manually first."
            echo "   Trying Railway CLI deployment anyway..."
            SERVICE_EXISTS=""
          else
            SERVICE_EXISTS=$(echo "$SERVICES_RESPONSE" | jq -r ".data.project.services.edges[].node | select(.name == \"${SERVICE_NAME}\") | .id" 2>/dev/null || echo "")
          fi
          
          if [ -z "$SERVICE_EXISTS" ] || [ "$SERVICE_EXISTS" == "null" ]; then
            echo "📦 Service doesn't exist, attempting to create via Railway API..."
            
            # Create service via GraphQL mutation
            CREATE_MUTATION='{"query":"mutation { serviceCreate(input: {name: \"'${SERVICE_NAME}'\", projectId: \"'${RAILWAY_PROJECT_ID}'\"}) { id name } }"}'
            CREATE_RESPONSE=$(curl -s -X POST \
              -H "Authorization: Bearer ${RAILWAY_TOKEN}" \
              -H "Content-Type: application/json" \
              -d "$CREATE_MUTATION" \
              "$RAILWAY_GRAPHQL" 2>&1 || echo "")
            
            # Check if creation succeeded
            if echo "$CREATE_RESPONSE" | jq -e '.data.serviceCreate.id' > /dev/null 2>&1; then
              SERVICE_ID=$(echo "$CREATE_RESPONSE" | jq -r '.data.serviceCreate.id')
              echo "✅ Service created successfully (ID: ${SERVICE_ID})"
              sleep 3
            else
              echo "⚠️ Service creation via API failed (requires Account Token)"
              echo "Response: ${CREATE_RESPONSE}"
              echo ""
              echo "💡 Trying Railway CLI - it may create service automatically..."
            fi
          else
            echo "✅ Service already exists (ID: ${SERVICE_EXISTS})"
          fi
          
          # Get the service ID (either from creation or existing)
          FINAL_SERVICE_ID="${SERVICE_ID:-${SERVICE_EXISTS}}"
          
          if [ -z "$FINAL_SERVICE_ID" ] || [ "$FINAL_SERVICE_ID" == "null" ]; then
            echo "❌ No service ID available, cannot proceed"
            exit 1
          fi
          
          echo "✅ Using service ID: ${FINAL_SERVICE_ID}"
          
          # Prepare repository information
          REPO_FULL="${GITHUB_REPOSITORY}"
          REPO_OWNER="${GITHUB_REPOSITORY_OWNER}"
          REPO_NAME="${GITHUB_REPOSITORY#*/}"
          echo "Repository: ${REPO_FULL} (${REPO_OWNER}/${REPO_NAME})"
          
          # Railway CLI uses RAILWAY_TOKEN environment variable automatically
          export RAILWAY_TOKEN="${RAILWAY_TOKEN}"
          
          # Note: Railway CLI's railway link is interactive and not suitable for CI/CD
          # Instead, we'll deploy directly using railway up with service specification
          # Railway CLI will use railway.toml for project context and deploy from current directory
          echo "💡 Ready to deploy - Railway CLI will use railway.toml for project context"
          
          # Create or verify public domain exists
          echo "🌐 Ensuring public domain is created..."
          
          # Railway CLI uses RAILWAY_TOKEN environment variable automatically
          # No need to call login command - just ensure token is set
          export RAILWAY_TOKEN="${RAILWAY_TOKEN}"
          
          # Check if domain exists via GraphQL
          DOMAIN_QUERY='{"query":"query { service(id: \"'${FINAL_SERVICE_ID}'\") { domains { edges { node { id domain } } } } }"}'
          DOMAIN_RESPONSE=$(curl -s -X POST \
            -H "Authorization: Bearer ${RAILWAY_TOKEN}" \
            -H "Content-Type: application/json" \
            -d "$DOMAIN_QUERY" \
            "$RAILWAY_GRAPHQL" 2>&1 || echo "")
          
          # Check if public domain exists
          PUBLIC_DOMAIN=$(echo "$DOMAIN_RESPONSE" | jq -r '.data.service.domains.edges[].node.domain // empty' 2>/dev/null | grep -E '\.up\.railway\.app$' | head -n1 || echo "")
          
          if [ -z "$PUBLIC_DOMAIN" ] || [ "$PUBLIC_DOMAIN" == "null" ]; then
            echo "📝 No public domain found, creating one..."
            
            # Get service's latest deployment (needed for domain creation)
            DEPLOYMENT_QUERY='{"query":"query { service(id: \"'${FINAL_SERVICE_ID}'\") { deployments { edges { node { id } } } } }"}'
            DEPLOYMENT_RESPONSE=$(curl -s -X POST \
              -H "Authorization: Bearer ${RAILWAY_TOKEN}" \
              -H "Content-Type: application/json" \
              -d "$DEPLOYMENT_QUERY" \
              "$RAILWAY_GRAPHQL" 2>&1 || echo "")
            
            DEPLOYMENT_ID=$(echo "$DEPLOYMENT_RESPONSE" | jq -r '.data.service.deployments.edges[0].node.id // empty' 2>/dev/null || echo "")
            
            if [ -n "$DEPLOYMENT_ID" ] && [ "$DEPLOYMENT_ID" != "null" ]; then
              echo "✅ Found deployment ID: ${DEPLOYMENT_ID}"
              
              # Try creating public domain via GraphQL mutation
              # Railway generates domains in format: {service-name}-production.up.railway.app
              CREATE_DOMAIN_MUTATION='{"query":"mutation { domainCreate(input: {serviceId: \"'${FINAL_SERVICE_ID}'\", deploymentId: \"'${DEPLOYMENT_ID}'\", public: true}) { id domain } }"}'
              CREATE_DOMAIN_RESPONSE=$(curl -s -X POST \
                -H "Authorization: Bearer ${RAILWAY_TOKEN}" \
                -H "Content-Type: application/json" \
                -d "$CREATE_DOMAIN_MUTATION" \
                "$RAILWAY_GRAPHQL" 2>&1 || echo "")
              
              if echo "$CREATE_DOMAIN_RESPONSE" | jq -e '.data.domainCreate.domain' > /dev/null 2>&1; then
                PUBLIC_DOMAIN=$(echo "$CREATE_DOMAIN_RESPONSE" | jq -r '.data.domainCreate.domain')
                echo "✅ Public domain created via GraphQL: ${PUBLIC_DOMAIN}"
              else
                echo "⚠️ GraphQL domain creation failed, trying alternative mutation..."
                echo "Response: ${CREATE_DOMAIN_RESPONSE}"
                
                # Try alternative mutation format (without deploymentId)
                CREATE_DOMAIN_MUTATION2='{"query":"mutation { domainCreate(input: {serviceId: \"'${FINAL_SERVICE_ID}'\", public: true}) { id domain } }"}'
                CREATE_DOMAIN_RESPONSE2=$(curl -s -X POST \
                  -H "Authorization: Bearer ${RAILWAY_TOKEN}" \
                  -H "Content-Type: application/json" \
                  -d "$CREATE_DOMAIN_MUTATION2" \
                  "$RAILWAY_GRAPHQL" 2>&1 || echo "")
                
                if echo "$CREATE_DOMAIN_RESPONSE2" | jq -e '.data.domainCreate.domain' > /dev/null 2>&1; then
                  PUBLIC_DOMAIN=$(echo "$CREATE_DOMAIN_RESPONSE2" | jq -r '.data.domainCreate.domain')
                  echo "✅ Public domain created (alternative method): ${PUBLIC_DOMAIN}"
                else
                  echo "⚠️ All GraphQL methods failed"
                  echo "Response: ${CREATE_DOMAIN_RESPONSE2}"
                  echo "💡 Domain will be created automatically after deployment, or manually in Railway dashboard"
                  echo "   Expected format: ${SERVICE_NAME}-production.up.railway.app"
                fi
              fi
            else
              echo "⚠️ No deployment found yet"
              echo "💡 Domain will be created automatically after first deployment completes"
              echo "   Expected format: ${SERVICE_NAME}-production.up.railway.app"
            fi
          else
            echo "✅ Public domain already exists: ${PUBLIC_DOMAIN}"
          fi
          
          # Get service details to find environment ID
          echo "🔍 Getting service environment ID..."
          SERVICE_QUERY='{"query":"query { service(id: \"'${FINAL_SERVICE_ID}'\") { id name environment { id } } }"}'
          SERVICE_DETAILS=$(curl -s -X POST \
            -H "Authorization: Bearer ${RAILWAY_TOKEN}" \
            -H "Content-Type: application/json" \
            -d "$SERVICE_QUERY" \
            "$RAILWAY_GRAPHQL" 2>&1 || echo "")
          
          ENVIRONMENT_ID=$(echo "$SERVICE_DETAILS" | jq -r '.data.service.environment.id // empty' 2>/dev/null || echo "")
          
          if [ -z "$ENVIRONMENT_ID" ] || [ "$ENVIRONMENT_ID" == "null" ]; then
            echo "⚠️ Could not get environment ID, trying to use project default environment..."
            # Try to get default environment from project
            ENV_QUERY='{"query":"query { project(id: \"'${RAILWAY_PROJECT_ID}'\") { environments { edges { node { id name } } } } }"}'
            ENV_RESPONSE=$(curl -s -X POST \
              -H "Authorization: Bearer ${RAILWAY_TOKEN}" \
              -H "Content-Type: application/json" \
              -d "$ENV_QUERY" \
              "$RAILWAY_GRAPHQL" 2>&1 || echo "")
            ENVIRONMENT_ID=$(echo "$ENV_RESPONSE" | jq -r '.data.project.environments.edges[0].node.id // empty' 2>/dev/null || echo "")
          fi
          
          if [ -z "$ENVIRONMENT_ID" ] || [ "$ENVIRONMENT_ID" == "null" ]; then
            echo "⚠️ Could not determine environment ID, variables may not be set correctly"
            echo "   Service exists but deployment may need manual configuration"
          else
            echo "✅ Using environment ID: ${ENVIRONMENT_ID}"
            
            # Set environment variables using Railway GraphQL API (correct format)
            echo "📝 Setting environment variables via GraphQL API..."
            
            # Set each environment variable via GraphQL mutation
            # USE_DATABASE=false enables mock mode (no database needed for PR previews)
            # PORT=8080 matches the app's default port (env.ts defaults to 8080)
            for var_pair in "NODE_ENV=preview" "PORT=8080" "PR_NUMBER=${PR_NUMBER}" "USE_DATABASE=false" "DB_NAME=myapp_pr_${PR_NUMBER}" "JWT_SECRET=preview-secret-${PR_NUMBER}"; do
              VAR_NAME="${var_pair%%=*}"
              VAR_VALUE="${var_pair#*=}"
              
              # Correct mutation format: environmentId, projectId, name (not key), value
              SET_VAR_MUTATION='{"query":"mutation { variableUpsert(input: {environmentId: \"'${ENVIRONMENT_ID}'\", projectId: \"'${RAILWAY_PROJECT_ID}'\", name: \"'${VAR_NAME}'\", value: \"'${VAR_VALUE}'\"}) }"}'
              VAR_RESPONSE=$(curl -s -X POST \
                -H "Authorization: Bearer ${RAILWAY_TOKEN}" \
                -H "Content-Type: application/json" \
                -d "$SET_VAR_MUTATION" \
                "$RAILWAY_GRAPHQL" 2>&1 || echo "")
              
              if echo "$VAR_RESPONSE" | jq -e '.data.variableUpsert == true' > /dev/null 2>&1; then
                echo "  ✅ Set ${VAR_NAME}=${VAR_VALUE}"
              else
                echo "  ⚠️ Failed to set ${VAR_NAME}: ${VAR_RESPONSE}"
              fi
            done
          fi
          
          # Trigger deployment using Railway CLI
          # Railway CLI automatically uses RAILWAY_TOKEN environment variable
          echo "📦 Deploying service to Railway..."
          
          # Railway CLI uses RAILWAY_TOKEN from environment automatically
          export RAILWAY_TOKEN="${RAILWAY_TOKEN}"
          
          # Set Railway service context via environment variable (if Railway CLI supports it)
          # This helps Railway CLI know which service to deploy to
          export RAILWAY_SERVICE="$SERVICE_NAME"
          
          echo "🚀 Deploying to service: ${SERVICE_NAME}..."
          echo "📤 Building and deploying from current directory..."
          
          # Railway CLI's `railway up` command builds from current directory and deploys
          # It uses railway.toml for project context
          # We're deploying from GitHub Actions workspace which has all the code
          
          # Try deploying with explicit service specification
          # Railway CLI syntax may vary, so we try multiple approaches
          DEPLOY_SUCCESS=false
          
          # Method 1: Try railway up with service flag
          if railway up --service "$SERVICE_NAME" --detach 2>&1; then
            echo "✅ Deployment triggered via railway up --service"
            DEPLOY_SUCCESS=true
          else
            echo "⚠️ railway up --service failed, trying alternative methods..."
            
            # Method 2: Try railway deploy command (if available)
            if railway deploy --service "$SERVICE_NAME" 2>&1; then
              echo "✅ Deployment triggered via railway deploy"
              DEPLOY_SUCCESS=true
            else
              # Method 3: Try railway up without service flag
              # Railway CLI should use railway.toml project context
              if railway up --detach 2>&1; then
                echo "✅ Deployment triggered via railway up"
                DEPLOY_SUCCESS=true
              else
                echo "⚠️ All Railway CLI deployment methods failed"
                echo ""
                echo "💡 Attempting deployment via Railway GraphQL API..."
                
                # Method 4: Try triggering deployment via GraphQL API
                # Create a deployment that builds from the service
                TRIGGER_MUTATION='{"query":"mutation { deploymentCreate(input: {serviceId: \"'${FINAL_SERVICE_ID}'\"}) { id status } }"}'
                TRIGGER_RESPONSE=$(curl -s -X POST \
                  -H "Authorization: Bearer ${RAILWAY_TOKEN}" \
                  -H "Content-Type: application/json" \
                  -d "$TRIGGER_MUTATION" \
                  "$RAILWAY_GRAPHQL" 2>&1 || echo "")
                
                if echo "$TRIGGER_RESPONSE" | jq -e '.data.deploymentCreate.id' > /dev/null 2>&1; then
                  DEPLOY_ID=$(echo "$TRIGGER_RESPONSE" | jq -r '.data.deploymentCreate.id')
                  echo "✅ Deployment triggered via GraphQL API (ID: ${DEPLOY_ID})"
                  DEPLOY_SUCCESS=true
                else
                  echo "⚠️ GraphQL deployment trigger also failed"
                  echo "Response: ${TRIGGER_RESPONSE}"
                  echo ""
                  echo "❌ All deployment methods failed"
                  echo "   Service: ${SERVICE_NAME} (ID: ${FINAL_SERVICE_ID})"
                  echo "   Repository: ${REPO_FULL}"
                  echo ""
                  echo "   Manual steps required:"
                  echo "   1. Go to: https://railway.app/project/${RAILWAY_PROJECT_ID}"
                  echo "   2. Click on service: ${SERVICE_NAME}"
                  echo "   3. Click 'Deploy' or 'Redeploy' button"
                  echo ""
                  echo "   After first manual deployment, connect repo for auto-deploy:"
                  echo "   - Settings → Connect GitHub repository → ${REPO_FULL}"
                  exit 1
                fi
              fi
            fi
          fi
          
          if [ "$DEPLOY_SUCCESS" = true ]; then
            echo "✅ Deployment initiated successfully!"
          fi
          
          # Wait for deployment to start
          echo "⏳ Waiting for deployment to start..."
          sleep 20
          
          # Query for the actual public domain (may have been created after deployment)
          echo "🔍 Verifying public domain..."
          FINAL_DOMAIN_QUERY='{"query":"query { service(id: \"'${FINAL_SERVICE_ID}'\") { domains { edges { node { id domain } } } } }"}'
          FINAL_DOMAIN_RESPONSE=$(curl -s -X POST \
            -H "Authorization: Bearer ${RAILWAY_TOKEN}" \
            -H "Content-Type: application/json" \
            -d "$FINAL_DOMAIN_QUERY" \
            "$RAILWAY_GRAPHQL" 2>&1 || echo "")
          
          ACTUAL_DOMAIN=$(echo "$FINAL_DOMAIN_RESPONSE" | jq -r '.data.service.domains.edges[].node.domain // empty' 2>/dev/null | grep -E '\.up\.railway\.app$' | head -n1 || echo "")
          
          if [ -n "$ACTUAL_DOMAIN" ] && [ "$ACTUAL_DOMAIN" != "null" ]; then
            PREVIEW_URL="https://${ACTUAL_DOMAIN}"
            echo "✅ Found public domain: ${ACTUAL_DOMAIN}"
          else
            # Fallback to expected format (Railway generates: {service-name}-production.up.railway.app)
            EXPECTED_DOMAIN="${SERVICE_NAME}-production.up.railway.app"
            PREVIEW_URL="https://${EXPECTED_DOMAIN}"
            echo "⚠️ Domain not found yet, using expected format: ${EXPECTED_DOMAIN}"
            echo "💡 Domain may still be provisioning, check Railway dashboard if URL doesn't work"
          fi
          
          echo "url=${PREVIEW_URL}" >> $GITHUB_OUTPUT
          echo "✅ Preview URL: ${PREVIEW_URL}"

      - name: Comment PR with preview URL
        uses: actions/github-script@v7
        with:
          script: |
            const url = '${{ steps.deploy.outputs.url }}';
            const pr = context.issue.number;
            await github.rest.issues.createComment({
              ...context.repo,
              issue_number: pr,
              body: `## 🚀 Preview Environment Ready\n\n**API URL:** ${url}\n\n⚠️ Ephemeral environment, deleted on PR close.`
            });
